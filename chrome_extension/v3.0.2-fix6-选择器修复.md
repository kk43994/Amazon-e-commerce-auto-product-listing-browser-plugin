# Chrome扩展 v3.0.2-fix6 选择器语法修复

## 问题描述 🐛

商品上传过程中出现错误：
```
商品 1 失败: ASIN搜索失败: Failed to execute 'querySelector' on 'Document':
'span:contains("搜索"), [class*="tab"]:contains("搜索")' is not a valid selector.
```

## 错误原因分析 📋

`:contains()` 是 jQuery 专用的伪选择器，不是标准的 CSS 选择器。原生 JavaScript 的 `querySelector()` 和 `querySelectorAll()` 方法不支持这个语法。

## 修复内容 ✅

### 1. amazon-navigator.js (第166-196行)

**原代码**：
```javascript
const searchTab = document.querySelector('span:contains("搜索"), [class*="tab"]:contains("搜索")');
```

**修复后**：
```javascript
// 方法1: 通过文本内容查找span
const spans = document.querySelectorAll('span');
for (const span of spans) {
    if (span.textContent.includes('搜索') || span.textContent.includes('検索') || span.textContent.includes('Search')) {
        searchTab = span;
        break;
    }
}

// 方法2: 如果没找到span，查找包含tab类的元素
if (!searchTab) {
    const tabs = document.querySelectorAll('[class*="tab"]');
    for (const tab of tabs) {
        if (tab.textContent.includes('搜索') || tab.textContent.includes('検索') || tab.textContent.includes('Search')) {
            searchTab = tab;
            break;
        }
    }
}
```

### 2. batch-upload-manager.js (第322行)

**原代码**：
```javascript
const saveButton = document.querySelector('button:contains("保存为草稿"), [aria-label*="保存"]');
```

**修复后**：
```javascript
// 方法1: 通过文本内容查找button
const buttons = document.querySelectorAll('button');
for (const button of buttons) {
    if (button.textContent.includes('保存为草稿') || button.textContent.includes('Save as Draft')) {
        saveButton = button;
        break;
    }
}

// 方法2: 通过aria-label查找
if (!saveButton) {
    saveButton = document.querySelector('[aria-label*="保存"]');
}
```

### 3. batch-upload-manager.js (第350行)

**原代码**：
```javascript
const saveButton = document.querySelector('button:contains("保存为草稿")');
```

**修复后**：
```javascript
const buttons = document.querySelectorAll('button');
for (const button of buttons) {
    if (button.textContent.includes('保存为草稿') ||
        button.textContent.includes('Save as Draft') ||
        button.textContent.includes('下書き保存')) {  // 添加日文支持
        saveButton = button;
        break;
    }
}
```

## 改进要点 🌟

1. **标准化选择器** - 使用原生 JavaScript API
2. **多语言支持** - 支持中文、英文、日文
3. **降级策略** - 多种查找方法确保找到元素
4. **性能优化** - 使用 break 提前退出循环

## 使用方法 📝

1. **重新加载插件**：
   - 打开 `chrome://extensions/`
   - 找到 "KK亚马逊电商自动铺货插件"
   - 点击 "重新加载" 按钮

2. **清除缓存**（如果问题仍存在）：
   - 右键点击插件图标
   - 选择 "管理扩展程序"
   - 关闭然后重新启用插件

3. **测试搜索功能**：
   - 访问亚马逊卖家后台
   - 打开插件
   - 输入ASIN进行搜索测试

## 兼容性保证 ✓

修复后的代码兼容：
- ✅ Chrome 60+
- ✅ Edge 79+
- ✅ Firefox 60+
- ✅ 所有现代浏览器

## 版本信息 📌

- **版本号**: v3.0.2-fix6
- **更新时间**: 2025-11-28
- **修复内容**: querySelector选择器语法错误
- **影响文件**:
  - amazon-navigator.js
  - batch-upload-manager.js

## 调试提示 🔍

如果仍有问题，可在控制台测试：

```javascript
// 测试查找搜索标签
const spans = document.querySelectorAll('span');
for (const span of spans) {
    if (span.textContent.includes('搜索')) {
        console.log('找到搜索标签:', span);
    }
}

// 测试查找保存按钮
const buttons = document.querySelectorAll('button');
for (const button of buttons) {
    if (button.textContent.includes('保存')) {
        console.log('找到保存按钮:', button);
    }
}
```

---

💡 **提示**: 这次修复确保了插件在所有浏览器环境下的兼容性，避免了jQuery依赖问题。